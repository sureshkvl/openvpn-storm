// Generated by CoffeeScript 1.7.1
(function() {
  var OpenvpnServerRegistry, OpenvpnService, OpenvpnUserRegistry;

  OpenvpnService = require('./openvpn-serivce');

  OpenvpnServerRegistry = require('./openvpn-registry'.OpenvpnServerRegistry);

  OpenvpnUserRegistry = require('./openvpn-registry'.OpenvpnUserRegistry);

  this.include = function() {
    var agent, plugindir, serverRegistry, userRegistry;
    agent = this.settings.agent;
    if (agent == null) {
      throw new Error("this plugin requires to be running in the context of a valid StormAgent!");
    }
    plugindir = this.settings.plugindir;
    if (plugindir == null) {
      plugindir = "/var/stormflash/plugins";
    }
    serverRegistry = new OpenvpnServerRegistry(plugindir + "/openvpn-servers.db");
    userRegistry = new OpenvpnUserRegistry(plugindir + "/openvpn-users.db");
    serverRegistry.on('ready', function() {
      var service, _i, _len, _ref, _results;
      _ref = this.list();
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        service = _ref[_i];
        if (!(service instanceof OpenvpnService)) {
          continue;
        }
        agent.log("restore: trying to recover:", service);
        _results.push((function(service) {
          return service.generate(function(err) {
            if (err != null) {
              return agent.log("restore: openvpn " + service.id + " failed to generate configs!");
            }
            return agent.invoke(service, function(err, instance) {
              if (err != null) {
                return agent.log("restore: openvpn " + service.id + " invoke failed with:", err);
              } else {
                return agent.log("restore: openvpn " + service.id + " invoke succeeded wtih " + instance);
              }
            });
          });
        })(service));
      }
      return _results;
    });
    this.post({
      '/openvpn/server': function() {
        var err, service;
        try {
          service = new OpenvpnService(null, this.body, {});
        } catch (_error) {
          err = _error;
          return this.next(err);
        }
        return service.generate((function(_this) {
          return function(err, results) {
            if (err != null) {
              return _this.next(err);
            }
            agent.log("POST /openvpn/server generation results:", results);
            serverRegistry.add(service);
            return agent.invoke(service, function(err, instance) {
              if (err != null) {
                return _this.next(err);
              }
              return _this.send({
                id: service.id,
                running: true
              });
            });
          };
        })(this));
      }
    });
    this.del({
      '/openvpn/server/:server': function() {
        var service;
        service = serverRegistry.get(this.param.server);
        if (service == null) {
          return this.send(404);
        }
        registry.remove(this.param.server);
        return this.send(204);
      }
    });
    this.post({
      '/openvpn/server/:server/users': function() {
        var server, serverId, tasks, user, users, _fn, _i, _len;
        serverId = this.params.server;
        users = this.body;
        server = serverRegistry.get(serverId);
        if (!((serverId != null) && (users != null) && (server != null))) {
          return this.send(400);
        }
        if (!(users instanceof Array)) {
          users = [users];
        }
        tasks = {};
        _fn = function(user) {
          return tasks[user.id] = function(callback) {
            var entry;
            user.ccdPath = server["client-config-dir"];
            entry = userRegistry.add(user);
            userRegistry.addUser(entry.data);
            return callback("Failed to add openvpn user! " + entry.data);
          };
        };
        for (_i = 0, _len = users.length; _i < _len; _i++) {
          user = users[_i];
          _fn(user);
        }
        return async.parallel(tasks, (function(_this) {
          return function(err, results) {
            if (err != null) {
              return _this.next(err);
            }
            return _this.send(results);
          };
        })(this));
      }
    });
    this.del({
      '/openvpn/server/:id/users/:user': function() {
        return vpn.deleteuser(this.params.id, this.params.user, (function(_this) {
          return function(res) {
            if (!(res instanceof Error)) {
              return _this.send(204);
            } else {
              return _this.next(new Error("Failed to delete openvpn user ! " + res));
            }
          };
        })(this));
      }
    });
    this.get({
      '/openvpn/server/:id': function() {
        var service;
        service = serverRegistry.get(this.param.id);
        if (service == null) {
          return this.send(404);
        } else {
          return this.send(service);
        }
      }
    });
    return this.get({
      '/openvpn/server': function() {
        return this.send(serverRegistry.list());
      }
    });
  };

}).call(this);
